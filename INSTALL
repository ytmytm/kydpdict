1. Rozpakuj ¼ród³a do wybranego katalogu

2. Kompilacja programu.
   wykonaj
    ./configure
   ewentualnie (zobacz ni¿ej komu mog³oby siê to przydaæ):
    ./configure --without-x

   wykonaj
    make

Je¶li skrypt ./configure zakoñczy siê b³êdem, to prawdopodobnie nie mo¿e znale¼æ ¶cie¿ek
do Qt. Nale¿y wówczas jawnie je podaæ, np.
    ./configure --with-qt-includes=/usr/X11R6/include/qt --with-qt-libs=/usr/X11R6/lib
Je¶li Qt zosta³o zainstalowane bezpo¶rednio ze ¼róde³ to prawdopodobnie wystarczy co¶
podobnego do:
    ./configure --with-qt-dir=/usr/local/qt

W razie k³opotów przeczytaj dokumentacje Qt (to o $QTDIR te¿ :).

Je¶li wywo³asz ./configure --without-x, to program bêdzie bezpo¶rednio zale¿a³ tylko
od biblioteki Qt. Mo¿e mieæ to zastosowanie na systemach, na których nie ma X11, ale
jest dostêpne Qt (MacOS X z natywnym Qt (nie-X11)?) oraz u tych purystów, którzy nie
chc± w ogóle niczego zwi±zanego z ryb± na tacce systemowej.

3. Instalacja programu.
   wykonaj
    make install

Program zostanie zainstalowany domy¶lnie do /usr/local lub innego drzewa okre¶lonego przez
opcjê --prefix skryptu ./configure.
Domy¶lnym katalogiem danych (z plikami s³ownika) jest /usr/local/share/kydpdict lub inny
wskazany przez opcjê --datadir do ./configure (dla --datadir DIR bêdzie to DIR/kydpdict).

UWAGA! Do dzia³ania programu niezbêdne jest posiadanie plików z danymi s³ownika z oryginalnej
p³yty YDP. Chodzi o pliki dict*.idx i dict*.dat. Nale¿y je umie¶ciæ gdzie¶ w dostêpnym miejscu
(np. /usr/share/ydpdict/ lub /usr/share/kydpdict/ )
